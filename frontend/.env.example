# Optional API URL for the companion Express backend.
# Leave empty to use MarketMentor with browser-only local storage.
# Include the /api prefix, e.g. http://localhost:5000/api
VITE_API_URL=http://localhost:5000/api
